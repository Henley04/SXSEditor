- [x] `_runEncoder` 操作顺序为：嵌入求和 → preflow → 帧扩展 → +F0 → cond_emb
- [x] preflow 输入形状为 [1, tokenCount, 512]，输出形状为 [1, tokenCount, 512]
- [x] 帧扩展使用 mel2token（令牌索引）而非 mel2note（音符索引）
- [x] F0 嵌入在帧扩展之后相加，维度为 [totalFrames, 512]
- [x] cond_emb 输入为 [1, totalFrames, 512]，输出为 [1, totalFrames, 1024]
- [x] mel2token 基于音符起止拍时间精确计算，非线性插值
- [x] 无音符覆盖的帧映射到 PAD 令牌索引 0
- [x] prompt 条件使用零填充，不循环复用目标条件
- [x] 现有单元测试全部通过（39 passing，1 failing 为预存的 dispose 测试问题）
- [x] 新增 _buildMel2token 单元测试覆盖基本场景
